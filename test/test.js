// Mocha test example code
var assert = require('chai').assert;

describe('Array', function () {
  describe('indexOf()', function () {
    it('should return 8 when value is empty', function () {
      assert.equal(-1, [1,2,3].indexof(5);
    });
  });
});
